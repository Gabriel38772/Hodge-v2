import { Box } from '@mui/material';
import Navbar from 'scenes/navbar';

const ProjectPage = () => {
    return <Box>
        <Navbar />
        projectpage
        </Box>;
};

export default ProjectPage;